apiVersion: v1
kind: Service
metadata:
  name: testrepo
  labels:
    app: testrepo
spec:
  selector:
    app: testrepo
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
